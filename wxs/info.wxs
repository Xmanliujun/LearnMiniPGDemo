 // js代码 
  var msg = "hello world";
  var name = "my name";
  
  var sum = function(num1,num2){
    return num1+num2
  }

  var test = function(){
    return "返回需要的内容结果"
  }

  // commonjs 的模块化导出的写法 
  module.exports = {
    msg:msg,
    name:name,
    sum:sum,
    test:test
  }

